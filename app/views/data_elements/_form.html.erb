<%= form_with(model: data_element, local: true) do |form| %>
  <% if data_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_element.errors.count, "error") %> prohibited this data_element from being saved:</h2>

      <ul>
      <% data_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :data_element_name %>
  </div>

  <div class="field">
    <%= form.label :data_set_id %>
    <%= form.text_field :data_set_id, id: :data_element_data_set_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
